syntax = "proto3";
option go_package = "github.com/mcfizh/grpc-demo/protos";

message RangeQuery {
   int32 minRange = 1;
   int32 maxRange = 2;
}

message ValueReply {
  int32 value = 1;
}

service Randomizer {
  rpc GetValues(RangeQuery) returns (stream ValueReply) {}
}
